<% @users.each do |user| %>
  <div>
    <img src="<%= user.photo %>" alt="User Photo">
    <h2><%= user.name %></h2>
    <p>Number of posts: <%= user.posts.count %></p>
    <%= link_to "#{user.name}'s Posts", user_path(user) %>
  </div>
   <br>
<% end %>


  

